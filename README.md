# context-org

TypeScript pnpm monorepo with three workspace packages and a root binary.

## Packages

- `@context-org/source`
- `@context-org/engine`
- `@context-org/view`

## Commands

```sh
pnpm install
pnpm build
pnpm start
```
